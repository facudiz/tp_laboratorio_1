#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

int getInt(int input,char message[], char eMessage[], int lowLimit);
int getString(char* input,char message[], char eMessage[], int lowLimit, int hiLimit);
